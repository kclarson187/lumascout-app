/**
 * /following route — renders the shared FollowListScreen which auto-
 * detects mode by pathname (/followers ↔ /following) and hits the
 * matching backend endpoint.
 */
export { default } from '../src/components/FollowListScreen';
